import { HashRouter } from 'react-router-dom';
import { WebComponentDemo } from './chat-app-sdk-example/index';

function App() {
  return (
    <HashRouter>
      <WebComponentDemo />
    </HashRouter>
  );
}

export default App;
